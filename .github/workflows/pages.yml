name: Build and deploy GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:
  schedule:
    # Po–Pá večer a ráno (UTC) — po jednání PS
    - cron: "30 20 * * 1-5"
    - cron: "0 4 * * 1-6"

concurrency:
  group: pages
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-svejk.txt

      - name: Install dependencies
        run: pip install -r requirements-svejk.txt

      - name: Refresh data from Hlídač (cron / ručně)
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        env:
          HLIDAC_TOKEN: ${{ secrets.HLIDAC_TOKEN }}
        run: |
          if [ -z "$HLIDAC_TOKEN" ]; then
            echo "HLIDAC_TOKEN není nastaven — používám committed processed/facts"
            exit 0
          fi
          ./run-svejk.sh build --obdobi 2025 --vsechny-schuze -q \
            || ./run-svejk.sh build --obdobi 2025 --vsechny-schuze --only align,extract,compose -q

      - name: Export static site
        env:
          SVEJK_BASE_PATH: ""
          SVEJK_PAGES_CNAME: poslusnehlasim.cz
          SVEJK_SITE_URL: https://poslusnehlasim.cz
          BUTTONDOWN_USERNAME: ${{ secrets.BUTTONDOWN_USERNAME }}
        run: ./run-svejk.sh export-pages --obdobi 2025 --out site --cname poslusnehlasim.cz

      - name: Restore newsletter state
        uses: actions/cache/restore@v4
        with:
          path: processed/newsletter-state.json
          key: newsletter-state-v1

      - name: Notify newsletter (Buttondown)
        continue-on-error: true
        env:
          BUTTONDOWN_API_KEY: ${{ secrets.BUTTONDOWN_API_KEY }}
          SVEJK_SITE_URL: https://poslusnehlasim.cz
          SVEJK_BASE_PATH: ""
        run: |
          if [ -z "$BUTTONDOWN_API_KEY" ]; then
            echo "BUTTONDOWN_API_KEY není nastaven — přeskočeno"
            exit 0
          fi
          ./run-svejk.sh newsletter-notify --obdobi 2025 || echo "Newsletter notify selhal — deploy pokračuje"

      - name: Save newsletter state
        uses: actions/cache/save@v4
        if: success()
        with:
          path: processed/newsletter-state.json
          key: newsletter-state-v1

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
